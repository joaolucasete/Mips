.text
li $s2, 10          	# carrega o valor IMEDIATO 10 no registrador s2 ( li = load immediate )
li $s3, 15		        # carrega o valor IMEDIATO 15 no registrador s3
add $s1, $s2, $s3	    # executa a soma de s2 com s3 e armazeno o resultado em s1
